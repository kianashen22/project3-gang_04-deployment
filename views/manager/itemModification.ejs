<!DOCTYPE html>
<head>
    <title>Item Modification</title>
</head>

<body>
    <h1>Item Modification</h1>
    <p><strong>ID:</strong> <%= item.beverage_info_id %></p>
    <p><strong>Name:</strong> <%= item.name %></p>
    <p><strong>Price:</strong> <%= item.price %></p>

    <div>
        <% for (var i in bev_items) { %>
        <p><%= bev_items[i].inventory_name, bev_items[i].quantity, bev_items[i].unit %></p>
        <% } %>
    </div>

    <div>
<!--        form for now, buttons will be added later to improve UX-->
        <form action="/manager/addToMenuItem" method="POST">
            <input type="hidden" name="id" value="<%= item.beverage_info_id %>">
            <label>Inventory Id:</label>
            <input type="number" id="inventory_id" name = "inv_id" required>
            <label>Quantity:</label>
            <input type="number" id="quantity" name = "qt" required>

<!--            consider a dropdown to prevent random units-->
            <label>Unit:</label>
            <input type="text" id="unit" name = "unit" required>

            <button type="submit">Add to Item</button>
        </form>

        <form action="/manager/removeFromMenuItem" method="POST">
            <input type="hidden" name="id" value="<%= item.beverage_info_id %>">

            <label>Inventory ID:</label>
            <input type="number" id="inventory_id" name = "inv_id" required>
            <button type="submit">Remove from Item</button>
        </form>
    </div>

    <a href="/manager/menuModification"> DONE </a>
</body>